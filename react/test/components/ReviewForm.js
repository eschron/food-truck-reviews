import React from 'react';
import ReviewForm from '../../src/components/ReviewForm';

describe('ReviewForm', () => {
  let wrapper;

  beforeEach(() => {
    jasmineEnzyme();
    wrapper = mount(<ReviewForm />);
  });

})
